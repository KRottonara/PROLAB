I think I am almost getting it. So my odometry that I use to calculate my control input is completely selfrelated. Hence I calculate directly the control input with the odometry data unrelated to anything else meaning that the calculcation represents 100% just another way the odometry is represented. The EKF model is depending on itself and the control data. This means that that what I add is not just the integration of some other representation of the pure control input but rather i add an integral of the control input in relation to how my previous belief affects it. This creates not just another representation that happens to contain trigonometric functions but rather it creates a new functions that is not linearly dependent on mu_t0.