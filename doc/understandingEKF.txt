The odometry that I utilize to calculate my control input is entirely self-related. Consequently, I directly calculate the control input using odometry data unrelated to any other factors, implying that the calculation represents 100% merely another representation of odometry. The EKF model relies on itself and the control data. This implies that what I add is not merely the integration of some other representation of the pure control input, but rather I add an integral of the control input in relation to how my previous belief affects it. This does not merely create another representation that happens to contain trigonometric functions, but rather it generates a novel function that is not linearly dependent on mu_t0.